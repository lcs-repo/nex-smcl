/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['your-image-hosting-domain.com'], // Add this if images are hosted externally
  },
}; 

export default nextConfig;
